<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    
    
      <style name="listview_divier_line">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">0.50dip</item>
        <item name="android:background">@color/listview_line_color</item>
    </style>
    <style name="listview_divier_line_bold">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">0.98dip</item>
        <item name="android:background">@color/listview_line_bold_color</item>
    </style>
      <style  name="common_btn_blue">
        <item name="android:background">@drawable/btn_blue_btn</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:gravity">center</item>
        <item name="android:minHeight">40dip</item>
        <item name="android:minWidth">50dip</item>
    </style>
</resources>
